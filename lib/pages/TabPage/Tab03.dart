import 'package:flutter/material.dart';

class Tab03Page extends StatelessWidget {
  const Tab03Page({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Tab ລົດ'),
    );
  }
}
