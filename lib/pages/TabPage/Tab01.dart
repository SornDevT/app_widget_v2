import 'package:flutter/material.dart';

class Tab01Page extends StatelessWidget {
  const Tab01Page({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Tab ລົດຖີບ'),
    );
  }
}
